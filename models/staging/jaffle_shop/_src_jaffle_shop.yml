sources:
  - name: jaffle_shop
    database: AFZAL_JASANI
    schema: RAW_JAFFLE_SHOP
    tables:
      - name: customers
      - name: orders
        config:
          freshness:
            warn_after:
              count: 24
              period: hour
            error_after: 
              count: 48
              period: hour
          loaded_at_field: _etl_loaded_at